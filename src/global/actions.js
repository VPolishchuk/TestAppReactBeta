import { createAction } from 'redux-act';
///////////////////////////////////////////////////////////////////////////////////////////////////

export const clearGlobalStore = createAction('clearGlobalStore');

export const getSportsTypeRequest = createAction('getSportsTypeRequest');
export const getSportsTypeSuccess = createAction('getSportsTypeSuccess');
