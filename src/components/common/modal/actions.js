import { createAction } from 'redux-act';
///////////////////////////////////////////////////////////////////////////////////////////////////

export const setOpenModal = createAction('setOpenModal');
export const setCloseModal = createAction('setCloseModal');
